const EActionTokenType = {
    ACTIVATE: "activate",
    FORGOT_PASSWORD: "forgot-password",
}

const ETokenType = {
    ACCESS: "access",
    REFRESH: "refresh",
}

module.exports = {
    EActionTokenType,
    ETokenType,
}